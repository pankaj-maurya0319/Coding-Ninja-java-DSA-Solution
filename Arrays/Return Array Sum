public static int sum(int[] arr) {
				//Your code goes here
         int summation =0;
        
        for(int i=0; i<arr.length; i++){
            
            summation += arr[i];
            
        }
        return summation;
